# Assignments
Assignments for Job Applications


#Commands used

  pip install --upgrade virtualenv
  virtualenv -p python3 envname
  pip install fastapi[all]
  pip install beautifulsoup4
  uvicorn main:app --reload
  pip install "uvicorn[standard]"
